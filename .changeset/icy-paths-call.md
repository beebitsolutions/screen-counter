---
'@beebit/screen-counter': minor
---

Extend the --verbose output of the CLI to also list every detected route with its canonical URL. The previous output only included modals and disabled components; routes were only visible via --json. Now `screen-counter . --verbose` shows the full picture in one go
