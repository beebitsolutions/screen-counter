/**
 * Fixture A.12 — Not-found UI for /dashboard.
 * Expected: does NOT count (not-found.tsx is a special file).
 */
export default function DashboardNotFound() {
  return <p>Dashboard section not found.</p>;
}
