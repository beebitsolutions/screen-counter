/**
 * Fixture A.9 — /dashboard page.
 * Expected: counts as 1 route.
 */
export default function DashboardPage() {
  return (
    <main>
      <h1>A.9 — /dashboard</h1>
      <p>Static route under <code>/dashboard</code>. Counts as 1.</p>
    </main>
  );
}
