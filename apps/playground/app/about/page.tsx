/**
 * Fixture A.2 — Static nested route.
 * Expected: counts as 1 route. Route: /about.
 */
export default function AboutPage() {
  return (
    <main>
      <h1>A.2 — /about</h1>
      <p>Static nested route. Counts as 1 route.</p>
    </main>
  );
}
